# doc_structure_version: 3.0

rules:
  root_dirs:
    - docs/rules/
  doc_types_map:
    docs/rules/: rule
  patterns:
    target_glob: "**/*.md"
    exclude: []

specs:
  root_dirs:
    - docs/specs/
  doc_types_map:
    docs/specs/: spec
  patterns:
    target_glob: "**/*.md"
    exclude: []
