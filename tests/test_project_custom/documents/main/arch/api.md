# API Architecture

This document uses custom directory names:
- "documents" instead of "specs"
- "arch" instead of "design"

## Endpoints

- GET /api/users
- POST /api/auth/login
- DELETE /api/sessions
