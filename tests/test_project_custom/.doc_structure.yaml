# doc_structure_version: 3.0

rules:
  root_dirs:
    - guidelines/
  doc_types_map:
    guidelines/: rule
  patterns:
    target_glob: "**/*.md"
    exclude: []

specs:
  root_dirs:
    - documents/
  doc_types_map:
    documents/: spec
  patterns:
    target_glob: "**/*.md"
    exclude: []
