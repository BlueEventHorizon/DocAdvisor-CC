# ToC Auto-Generation Configuration
# Generated by Doc Advisor setup script
# doc-advisor-version-xK9XmQ: {{DOC_ADVISOR_VERSION}}
#
# root_dirs and exclude: Derived from .doc_structure.yaml at runtime.
#   To override, add root_dirs explicitly under rules/specs section.
#   exclude in patterns below is merged with .doc_structure.yaml excludes.

# === rules configuration ===
rules:
  # root_dirs: []    # Uncomment to override .doc_structure.yaml
  toc_file: .claude/doc-advisor/toc/rules/rules_toc.yaml
  checksums_file: .claude/doc-advisor/toc/rules/.toc_checksums.yaml
  work_dir: .claude/doc-advisor/toc/rules/.toc_work/

  patterns:
    target_glob: "**/*.md"
    exclude: []    # Additional excludes (merged with .doc_structure.yaml)

  output:
    header_comment: "Development documentation search index for rules-advisor subagent"
    metadata_name: "Development Documentation Search Index"

# === specs configuration ===
specs:
  # root_dirs: []    # Uncomment to override .doc_structure.yaml
  toc_file: .claude/doc-advisor/toc/specs/specs_toc.yaml
  checksums_file: .claude/doc-advisor/toc/specs/.toc_checksums.yaml
  work_dir: .claude/doc-advisor/toc/specs/.toc_work/

  patterns:
    target_glob: "**/*.md"
    exclude: []    # Additional excludes (merged with .doc_structure.yaml)

  output:
    header_comment: "Project specification document search index for specs-advisor subagent"
    metadata_name: "Project Specification Document Search Index"

# === common configuration ===
common:
  parallel:
    max_workers: 5           # Concurrent subagents (reduce to 3 for 100+ files to avoid rate limits)
    fallback_to_serial: true
