# ToC Auto-Generation Configuration
# Generated by Doc Advisor setup script
# doc-advisor-version-xK9XmQ: {{DOC_ADVISOR_VERSION}}

# === rules configuration ===
rules:
  root_dir: .claude/doc-advisor/docs/rules
  toc_file: .claude/doc-advisor/toc/rules/rules_toc.yaml
  checksums_file: .claude/doc-advisor/toc/rules/.toc_checksums.yaml
  work_dir: .claude/doc-advisor/toc/rules/.toc_work/

  patterns:
    target_glob: "**/*.md"
    exclude:
      # - reference
      # - archive

  output:
    header_comment: "Development documentation search index for rules-advisor subagent"
    metadata_name: "Development Documentation Search Index"

# === specs configuration ===
specs:
  root_dir: .claude/doc-advisor/docs
  toc_file: .claude/doc-advisor/toc/specs/specs_toc.yaml
  checksums_file: .claude/doc-advisor/toc/specs/.toc_checksums.yaml
  work_dir: .claude/doc-advisor/toc/specs/.toc_work/

  patterns:
    target_dirs:
      requirement: {{REQUIREMENT_DIR_NAME}}
      design: {{DESIGN_DIR_NAME}}
    exclude:
      - {{PLAN_DIR_NAME}}  # Read in full during work, no search needed
      - rules              # Scanned separately by rules config
      # - reference
      # - /info/

  output:
    header_comment: "Requirements and design document search index for specs-advisor subagent"
    metadata_name: "Requirements and Design Document Search Index"

# === common configuration ===
common:
  parallel:
    max_workers: 5
    fallback_to_serial: true

# === external sources (optional) ===
# Add external document sources via git submodule or local symlink.
# Run /sync-docs after editing this section.
#
# external_sources:
#   rules:
#     - name: org-standards
#       type: git
#       url: https://github.com/org/standards.git
#       branch: main
#       sparse_path: rules/
#     - name: shared-rules
#       type: local
#       path: /shared/standards
#   requirements:
#     - name: partner-specs
#       type: git
#       url: https://github.com/partner/specs.git
#   design:
#     - name: partner-design
#       type: git
#       url: https://github.com/partner/specs.git
#       sparse_path: design/
